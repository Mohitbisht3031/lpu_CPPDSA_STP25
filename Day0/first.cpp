#include<bits/stdc++.h> //header file
using namespace std; // namespace group

int main(){  // main function from which all the cpp code execution starts
    cout<<"Hey welcome to the 0 class using a custom name for the output file"<<endl; // to give output

    // to take an input
    int a;
    cin >> a;

    return 0;
}